require 'spec_helper'

describe Runner do
  let(:runner) { Runner.new }
  
  it "should create a game" do
    pending "rewriting this to check if a game exists"
    runner.game.should_not be_nil
  end
end